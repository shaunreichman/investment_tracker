/**
 * Shared Form Validation Schemas - Barrel Export
 * 
 * Central export point for all shared form validation schemas.
 * These schemas can be composed into domain-specific schemas.
 * 
 * @module shared/hooks/schemas
 */

// Re-export all shared schemas
export * from './sharedSchemas';

