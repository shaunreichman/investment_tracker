/**
 * Banking Domain Routes
 * 
 * Defines React Router routes for the banking domain.
 * 
 * Currently empty as banking domain has no UI pages/components.
 * Banking functionality is API/hooks/types only.
 * 
 * Usage:
 *   import { bankRoutes } from '@/bank/routes';
 */

import { RouteObject } from 'react-router-dom';

/**
 * Banking domain routes.
 * 
 * Empty array as banking has no UI pages - it's API/hooks/types only.
 */
export const bankRoutes: RouteObject[] = [];

