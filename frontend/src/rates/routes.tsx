/**
 * Rates Domain Routes
 * 
 * Defines React Router routes for the rates domain.
 * 
 * Currently empty as rates domain has no UI pages/components.
 * Rates functionality is API/hooks/types only.
 * 
 * Usage:
 *   import { ratesRoutes } from '@/rates/routes';
 */

import { RouteObject } from 'react-router-dom';

/**
 * Rates domain routes.
 * 
 * Empty array as rates has no UI pages - it's API/hooks/types only.
 */
export const ratesRoutes: RouteObject[] = [];

