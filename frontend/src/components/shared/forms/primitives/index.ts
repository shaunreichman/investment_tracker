/**
 * Form Primitives - Low-level form input components
 * 
 * These are form-library-agnostic controlled components that provide
 * specialized input behavior (formatting, validation, etc.) without
 * being tied to React Hook Form or any specific form library.
 * 
 * Higher-level components (Form*Field) wrap these primitives to
 * integrate them with React Hook Form.
 */

export { NumberInputField } from './NumberInputField';
export type { NumberInputFieldProps } from './NumberInputField';

